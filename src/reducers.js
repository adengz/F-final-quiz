const defaultState = {
  ungrouped: {
    trainers: {},
    trainees: {},
  },
  groups: {},
};

const reducers = (state = defaultState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducers;
